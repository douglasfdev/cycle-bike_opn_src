_format_version: "3.0"

services:
  - name: cycle-bike-service
    url: http://cycle-bike:5161
    routes:
      - name: main-route
        paths:
          - /api
        strip_path: true

    plugins:
      - name: jwt
        config:
          claims_to_verify:
            - exp